# Used both by docker-compose.yml (to initialize the Postgres container)
# and inside DATABASE_URL below (must match) -- change the password.
POSTGRES_USER=receiptapp
POSTGRES_PASSWORD=YOUR_PASSWORD
POSTGRES_DB=receiptanalyser

# "db" is the Postgres service name in docker-compose.yml -- Docker's internal
# networking resolves it automatically, no IP address needed.
DATABASE_URL=postgresql+psycopg2://receiptapp:YOUR_PASSWORD@db:5432/receiptanalyser
